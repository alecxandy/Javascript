let isvalid = true;



let zero = 0
    //com ternario
const resultado = zero > 0 ? 0 : -1

//arrow function
const resltsoma = function(x, y) {
    return x + y;
}

//arrow function mais simplificada
const dividir = (x, y) => {
    return x / y;
}