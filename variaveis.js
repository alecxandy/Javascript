numero = 1; //fazendo hosting onde atribuimos variveis antes de declarar.
            //variaveis do tipo let não podem ser iniciadas antes de declaradas
console.log(numero + 3);
var numero;
let variaveavelLet = 2 // são variaveis que só funcionam em blocos
const constantes = 0 // variaveis que não podem ser alteradas


let nome = 'alexandre'
let sobrenome = 'gomes'
console.log(nome.concat(sobrenome))


