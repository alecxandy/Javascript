let isvalid = true;
let zero = 0

//sem ternario
if (zero > 0) {
    console.log(0)
} else {
    console.log(-1)
}

//com ternario
const resultado = zero > 0 ? 0 : -1

//arrow function
const resultsoma = function(x, y) {
    return x + y;
}

//arrow function mais simplificada
const dividir = (x, y) => {
    return x / y;
}